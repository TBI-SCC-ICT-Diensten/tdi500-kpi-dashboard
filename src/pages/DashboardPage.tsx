import { Box, Typography } from '@mui/material';

const DashboardPage = () => {
  return (
    <Box>
      <Typography variant="h5" gutterBottom>
        Dashboard
      </Typography>
      <Typography color="text.secondary">
        Dashboard overzicht — wordt binnenkort geïmplementeerd.
      </Typography>
    </Box>
  );
};

export default DashboardPage;
